[package]
name = "dejadoc"
version = "0.1.1"
edition = "2024"
rust-version = "1.88"
authors = ["Luca Cappelletti"]
description = "Find duplicated Rust doctests across a workspace"
license = "MIT"
repository = "https://github.com/LucaCappelletti94/cargo-dejadoc"
keywords = ["cargo-subcommand", "doctest", "duplicate-detection", "cli", "rustdoc"]
categories = ["command-line-utilities", "development-tools::testing"]

[[bin]]
name = "cargo-dejadoc"
path = "src/main.rs"
required-features = ["std"]

[features]
default = ["std"]
std = ["dep:cargo_metadata", "dep:clap", "dep:thiserror", "dep:toml"]

[dependencies]
blake3 = { version = "1.8.7", default-features = false }
cargo_metadata = { version = "0.23.1", optional = true }
clap = { version = "4.6.6", features = ["derive"], optional = true }
proc-macro2 = { version = "1.0.107", default-features = false, features = ["span-locations"] }
quote = { version = "1.0.47", default-features = false }
serde = { version = "1.0.229", default-features = false, features = ["derive", "alloc"] }
serde_json = { version = "1.0.151", default-features = false, features = ["alloc"] }
syn = { version = "3.0.5", default-features = false, features = ["full", "parsing", "printing", "clone-impls", "visit-mut"] }
thiserror = { version = "2.0.20", optional = true }
toml = { version = "1.1.6", optional = true }

[dev-dependencies]
tempfile = "3.27.0"

[lints.rust]
missing_docs = "forbid"
unused_macro_rules = "forbid"
unconditional_recursion = "forbid"
unreachable_patterns = "forbid"
unused_import_braces = "forbid"
unused_must_use = "forbid"
unsafe_code = "forbid"
deprecated = "deny"
unknown_lints = "allow"

[lints.rustdoc]
broken_intra_doc_links = "forbid"
bare_urls = "forbid"
invalid_codeblock_attributes = "forbid"
invalid_html_tags = "forbid"
missing_crate_level_docs = "forbid"
unescaped_backticks = "forbid"

[lints.clippy]
all = { level = "deny", priority = -1 }
pedantic = { level = "deny", priority = -1 }
allow_attributes = "deny"
allow_attributes_without_reason = "deny"

